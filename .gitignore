# MATLAB generated files
*.asv
*.m~
temp_result.mat
my_checkpoint.mat
*_checkpoint.mat
checkpoint*.mat

# Generated solver summaries
results_summary.csv
results_par_summary.csv
results_parfeval_summary.csv

# Raw benchmark work directories stay local. Curated reports, summary CSV files,
# plots, and metadata are tracked separately; portable raw ZIP files are release assets.
benchmark_results_v5/
scheduler_benchmark_v5/
# Optional historical v4 connectivity inputs are local-only and can be large.
benchmark_results_20260715/
tmp/
temp_result_policy_test.mat

# Release archives are attached to the GitHub Release, not committed to Git.
release_assets/*.zip
release_assets/*.sha256
release_assets/*.mexw64

# LaTeX intermediates
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# OS/editor noise
.DS_Store
Thumbs.db
desktop.ini
